# Copy to .env.local and fill in values

# Backend API URL (empty = same origin via Vite proxy in dev)
VITE_API_URL=

# Set in production to your Worker URL
# VITE_API_URL=https://group-trip-planner.your-subdomain.workers.dev
